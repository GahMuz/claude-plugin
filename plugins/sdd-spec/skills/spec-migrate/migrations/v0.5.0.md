# v0.5.0

Aucune migration nécessaire.

Cette version introduit `context.md` par spec (sauvegardé via `/spec close`). Le fichier est facultatif à l'ouverture — si absent, le plugin reconstitue le contexte depuis `log.md` + `state.json`. Les specs existantes continuent de fonctionner sans modification.
