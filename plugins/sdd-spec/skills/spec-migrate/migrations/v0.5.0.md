La permission d'écriture est bloquée. Voici le contenu du fichier à créer :

**`plugins/sdd-spec/skills/spec-migrate/migrations/v0.5.0.md`**

```markdown
# v0.5.0

Aucune migration nécessaire.
```

**Justification :** La seule nouveauté de schéma est le fichier `context.md` par spec. Ce fichier est :
- créé automatiquement par `/spec close` — aucune action manuelle requise
- facultatif à l'ouverture : si absent, le plugin reconstitue le contexte depuis `log.md` + `state.json`

Les specs existantes continuent de fonctionner sans modification. Le `context.md` se matérialisera naturellement au prochain `/spec close`.
