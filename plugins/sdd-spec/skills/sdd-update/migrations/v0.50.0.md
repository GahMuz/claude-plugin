The diff shows a documentation-only change: the `state.json` section in `SKILL.md` gained a note clarifying that the existing `schemaVersion` field serves the same migration-detection role as in `config.json`. The `state.json` schema in `templates.md` already contained `"schemaVersion": "1.0.0"` — no field was added, renamed, or removed from any user-facing JSON file.

```markdown
# v0.50.0

Aucune migration nécessaire.
```
