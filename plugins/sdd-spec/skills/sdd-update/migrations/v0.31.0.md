The diffs reveal:
- `sdd-init/SKILL.md` is a rename of `spec-init/SKILL.md` with only the skill name and invocation command updated (`/spec-init` → `/sdd-init`, `/spec-status` → `/sdd-status`). The `config.json` schema is identical between old and new.
- `spec-format/SKILL.md` has a single cosmetic change in a comment: `from /spec-init` → `from /sdd-init`.

No `.sdd/` data schema changed. Here is the migration file:

```markdown
# v0.31.0

Aucune migration nécessaire.
```
