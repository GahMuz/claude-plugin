The change is clear: the model key `"graph-builder"` was renamed to `"graph-builder-java"` in `.sdd/config.json`. Here is the migration file content:

---

# v0.49.0 — Renommage de la clé modèle `graph-builder` en `graph-builder-java`

La clé `graph-builder` dans le bloc `models` de `.sdd/config.json` est renommée en `graph-builder-java` pour refléter la nature spécifique à Java du builder de graphes.

## 1. Vérifier si la clé obsolète est présente

```bash
grep '"graph-builder"' .sdd/config.json
```

Si la commande ne retourne rien, aucune action requise.

## 2. Renommer la clé dans le bloc `models`

Dans `.sdd/config.json`, remplacer dans le bloc `models` :

```json
"graph-builder": "haiku"
```

par :

```json
"graph-builder-java": "haiku"
```

> Note : si vous aviez personnalisé la valeur du modèle (ex. `"sonnet"`), conserver votre valeur et remplacer uniquement la clé.

## 3. Mettre à jour `schemaVersion`

Dans `.sdd/config.json`, mettre à jour :

```json
"schemaVersion": "0.49.0"
```
