The diff shows only internal plugin metadata changes (skill `name` normalized to kebab-case, `allowed-tools` added). Neither change affects user project data in `.sdd/`.

```markdown
# v0.34.0

Aucune migration nécessaire.
```
