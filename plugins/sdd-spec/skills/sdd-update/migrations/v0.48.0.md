The diff is clear. Two new fields — `moduleThreshold` and `serviceThreshold` — were added to the `graph` block in `config.json`. Here is the migration file content:

---

# v0.48.0 — Ajout des seuils de découpage du graphe

Deux nouveaux champs sont requis dans le bloc `graph` de `.sdd/config.json` pour contrôler le découpage par sous-package lors de la construction des graphes `sdd-graph`.

## 1. Vérifier si les champs sont déjà présents

```bash
grep -E "moduleThreshold|serviceThreshold" .sdd/config.json
```

Si la commande ne retourne rien, appliquer l'étape suivante.

## 2. Ajouter `moduleThreshold` et `serviceThreshold` dans le bloc `graph`

Dans `.sdd/config.json`, remplacer dans le bloc `graph` :

```json
"stalenessThresholdDays": 7
```

par :

```json
"stalenessThresholdDays": 7,
"moduleThreshold": 25,
"serviceThreshold": 30
```

## 3. Mettre à jour `schemaVersion`

Dans `.sdd/config.json`, mettre à jour :

```json
"schemaVersion": "0.48.0"
```
