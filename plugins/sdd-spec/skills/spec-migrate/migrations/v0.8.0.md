Le fichier existe déjà (vide, 1 ligne). Voici le contenu à y écrire :

---

```markdown
# v0.8.0 — Suppression du champ richRequirements

Le champ `richRequirements` est retiré de `.sdd/config.json` (il était toujours `false` par défaut et n'influençait aucun comportement).

## Step 1 : Vérifier la présence du champ

```bash
grep -q '"richRequirements"' .sdd/config.json \
  && echo "Migration nécessaire." \
  || { echo "Champ absent — rien à faire."; exit 0; }
```

## Step 2 : Supprimer le champ

Ouvrir `.sdd/config.json` via Edit et retirer la ligne :

```json
"richRequirements": false,
```

(ou `true` si l'utilisateur avait répondu oui lors du `/spec-init`).

## Step 3 : Mettre à jour schemaVersion

Dans `.sdd/config.json`, remplacer :

```json
"schemaVersion": "0.7.0"
```

par :

```json
"schemaVersion": "0.8.0"
```
```

---

**Justification du choix :**

- `richRequirements` était un champ booléen ajouté à `config.json` lors du `/spec-init`. Sa valeur par défaut était `false` et le plugin ne l'utilisait pas en pratique pour modifier le comportement des phases — d'où sa suppression.
- La migration est minimale : un grep pour détecter si la clé est présente, une suppression de ligne via Edit, et la mise à jour de `schemaVersion`.
- Pas de migration des specs elles-mêmes (`.sdd/specs/`) — seul `config.json` est affecté.
