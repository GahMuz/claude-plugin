La permission d'écriture est refusée. Voici le contenu à écrire dans `plugins/sdd-spec/skills/spec-migrate/migrations/v0.25.0.md` :

```markdown
# v0.25.0

Aucune migration nécessaire.
```

**Raisonnement :**

- La v0.24.0 a déjà appliqué toutes les modifications de données : réaffectation des modèles `orchestrator` et `code-reviewer`, ajout des 5 nouveaux agents dans `.models`.
- La v0.25.0 ne met à jour que des fichiers internes du plugin (`spec-init/SKILL.md`, agents, références de skills) — aucune structure `.sdd/` ou `.specs/` n'est touchée.
- Le migrator gère le bump de `schemaVersion` automatiquement quand il lit "Aucune migration nécessaire." (cf. SKILL.md Step 3.2).
