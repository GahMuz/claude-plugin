Voici le contenu que je veux écrire dans `v0.12.0.md` :

```markdown
# v0.12.0 — Création du répertoire .sdd/local/

## Step 1 : Créer le répertoire local

Le hook `spec-sync-check.sh` lit `.sdd/local/active.json` depuis la v0.11.0, mais ce répertoire n'était pas créé pour les projets déjà initialisés.

```bash
mkdir -p .sdd/local
```

## Step 2 : Exclure .sdd/local/ du suivi git

Ajouter `.sdd/local/` dans `.gitignore` si absent :

```bash
grep -qxF '.sdd/local/' .gitignore || echo '.sdd/local/' >> .gitignore
```

## Step 3 : Mettre à jour schemaVersion

Dans `.sdd/config.json`, remplacer `"schemaVersion": "0.11.0"` par `"schemaVersion": "0.12.0"`.
```

**Raisonnement :**

- **Step 1** : `.sdd/local/` est consommé par le hook depuis v0.11.0 mais jamais créé sur les projets existants — c'est un correctif concret.
- **Step 2** : Le diff spec-init ajoute explicitement `.sdd/local/` au `.gitignore`. Les projets existants n'ont pas cette ligne.
- **Step 3** : Bump schemaVersion standard.

Le changement `SUSPEND → CLOSE` dans les règles de maintenance de `registry.md` est purement documentaire — aucune donnée à transformer.
