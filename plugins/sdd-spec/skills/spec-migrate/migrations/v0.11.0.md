# v0.11.0

Aucune migration de données nécessaire.

Cette version introduit des corrections et améliorations internes :
- Correction du hook `spec-sync-check.sh` (lecture de `.sdd/local/active.json` au lieu de `.specs/`)
- Correction des `subagent_type` : `spec-driven-dev:*` → `sdd-spec:*` dans tous les agents
- Suppression du statut `suspended` du registre et de spec-format
- Réorganisation des fichiers de référence dans `spec/references/`

Les fichiers `.sdd/` restent compatibles sans modification.

## Step 1 : Mettre à jour schemaVersion

Dans `.sdd/config.json`, remplacer `"schemaVersion": "0.10.0"` par `"schemaVersion": "0.11.0"`.
