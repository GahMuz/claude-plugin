Le fichier existe mais est vide (1 ligne). Voici le contenu à écrire — demande l'autorisation d'écriture pour procéder :

---

```markdown
# v0.24.0 — Mise à jour de la section `models` dans config.json

Cette version réaffecte les modèles par défaut de deux agents et ajoute cinq nouvelles entrées d'agents dans `.sdd/config.json`.

## Step 1 : Mettre à jour les modèles existants

Dans `.sdd/config.json`, corriger les deux valeurs modifiées :

```bash
jq '.models.orchestrator = "sonnet" | .models["code-reviewer"] = "opus"' \
  .sdd/config.json > .sdd/config.tmp.json && mv .sdd/config.tmp.json .sdd/config.json
```

## Step 2 : Ajouter les nouveaux agents

Ajouter les cinq entrées manquantes si elles sont absentes :

```bash
jq '
  .models.planner //= "haiku" |
  .models["design-validator"] //= "sonnet" |
  .models["spec-reviewer"] //= "sonnet" |
  .models["doc-generator"] //= "haiku" |
  .models["doc-analyser"] //= "sonnet"
' .sdd/config.json > .sdd/config.tmp.json && mv .sdd/config.tmp.json .sdd/config.json
```

## Step 3 : Mettre à jour schemaVersion

Dans `.sdd/config.json`, remplacer `"schemaVersion": "0.23.0"` par `"schemaVersion": "0.24.0"`.
```

---

**Raisonnement des choix :**

- **Step 1** : `orchestrator` passe de `opus` → `sonnet` et `code-reviewer` de `sonnet` → `opus` — ce sont des valeurs existantes à corriger, pas de `//=` pour ne pas laisser l'ancienne valeur si la clé est présente.
- **Step 2** : Les 5 nouveaux agents utilisent `//=` (affectation si absent) pour ne pas écraser une surcharge utilisateur éventuelle.
- **Step 3** : Bump schemaVersion standard.

Autorise l'écriture pour que je pose le fichier.
